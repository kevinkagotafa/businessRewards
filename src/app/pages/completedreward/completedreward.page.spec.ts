import { ComponentFixture, TestBed } from '@angular/core/testing';
import { CompletedrewardPage } from './completedreward.page';

describe('CompletedrewardPage', () => {
  let component: CompletedrewardPage;
  let fixture: ComponentFixture<CompletedrewardPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(CompletedrewardPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
